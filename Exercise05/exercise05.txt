The recursive Fibonacci function is parallelized using OpenMP tasks. The calculations of fib(n-1) and fib(n-2) are created as separate tasks and may be executed concurrently by different threads. #pragma omp taskwait ensures both tasks finish before their results are added. A single region is used so that only one thread initially starts the recursive Fibonacci calculation.
